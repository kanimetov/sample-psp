# DTO Rules

## When DTO is changed:

1. **Find all usages:**
   ```bash
   grep -r "YourDtoName" psp-service/src/
   ```

2. **Update method comments:**
   - `@param` descriptions in services/controllers
   - `@return` descriptions for response DTOs

3. **Check compilation:**
   ```bash
   gradle compileJava
   ```

## Files to check:
- Service interfaces (`*Service.java`)
- Service implementations (`*ServiceImpl.java`)
- Controllers (`*Controller.java`)
